language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
scripts:
  - yarn build
deploy:
  local_dir: dist
  provider: pages
  token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
